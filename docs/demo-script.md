# Final Demo Script (3-5 Minutes)

1. Show repository structure and explain layered architecture.
2. Open Swagger docs (`/docs`) and execute core REST endpoints.
3. Execute third-party integration endpoints:
   - `GET /{id}/tracking`
   - `GET /students/{studentId}/recommendations`
4. Open `/graphql` and run:
   - events query with filtering/pagination
   - create/update/delete event mutations
   - capacityInsights query
5. Show test run output (`npm test`) and explain coverage.
6. Show deployed URL and OpenAPI/GraphQL links.
7. Summarize REST vs GraphQL tradeoffs and future improvements.
